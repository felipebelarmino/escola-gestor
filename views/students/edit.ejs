<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Aluno</title>
    <link rel="stylesheet" href="/css/students-edit.css">
</head>

<body>
    <header class="header">
        <h1>Editar Aluno</h1>
    </header>
    <main class="container">
        <form action="/students/<%= student.id %>?_method=PATCH" method="POST">
            <label for="name">Nome:</label>
            <input type="text" id="name" name="name" value="<%= student.name %>" required>

            <label for="age">Idade:</label>
            <input type="number" id="age" name="age" value="<%= student.age %>" required>

            <label for="class">Classe:</label>
            <input type="text" id="class" name="class" value="<%= student.class %>" required>

            <div class="form-actions">
                <a href="/students" class="btn btn-cancel">Cancelar</a>
                <button type="submit" class="btn btn-save">Atualizar</button>
            </div>
        </form>
    </main>
</body>

</html>