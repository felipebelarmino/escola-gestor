<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Presença</title>
    <link rel="stylesheet" href="/css/attendance.css">
</head>

<body>
    <header class="header">
        <div class="header-left">
            <!-- Ícone para voltar ao Controle de Presenças -->
            <a href="/attendance/<%= student.id %>" class="logo">
                <img src="https://5.imimg.com/data5/TX/KH/GLADMIN-9740327/online-school-management-software-500x500.jpg"
                    alt="Escola Gestor">
            </a>
        </div>
        <h1>Editar Presença de <%= student.name %>
        </h1>
    </header>

    <main class="container">
        <form action="/attendance/<%= attendance.id %>?_method=PATCH" method="POST" class="attendance-form">
            <label for="date">Data</label>
            <input type="date" id="date" name="date" value="<%= attendance.date %>" required>

            <label for="isPresent">Presente</label>
            <select id="isPresent" name="isPresent" required>
                <option value="true" <%=attendance.isPresent===true ? 'selected' : '' %>>Sim</option>
                <option value="false" <%=attendance.isPresent===false ? 'selected' : '' %>>Não</option>
            </select>

            <label for="remarks">Observações</label>
            <textarea id="remarks" name="remarks"
                placeholder="Adicione observações (opcional)"><%= attendance.remarks || '' %></textarea>

            <div class="form-actions">
                <a href="/attendance/<%= student.id %>" class="btn btn-secondary">Cancelar</a>
                <button type="submit" class="btn btn-save">Salvar Alterações</button>
            </div>
        </form>
    </main>
</body>

</html>